diff --git a/configure.ac b/configure.ac
index 70d2e82..9c44832 100644
--- a/configure.ac
+++ b/configure.ac
@@ -152,6 +152,13 @@ AS_IF([test "x$enable_debug" = "xyes"], [
         AC_DEFINE(ENABLE_DEBUG, [1], [Debug messages.])
 ])
 
+AC_ARG_ENABLE([prefix],
+        AS_HELP_STRING([--enable-prefix], [enable symbol prefix @<:@default=disabled@:>@]),
+        [], [enable_prefix=no])
+AS_IF([test "x$enable_prefix" = "xyes"], [
+        AC_DEFINE(ISAL_SYMBOL_PREFIX, [1], [Add symbol prefix to avoid symbol conflict.])
+])
+
 AC_ARG_ENABLE([programs],
 	AS_HELP_STRING([--enable-programs], [build test programs @<:@default=disabled@:>@]),
 	[], [enable_programs=no])
diff --git a/erasure_code/ec_base.c b/erasure_code/ec_base.c
index 92e066f..b9e3823 100644
--- a/erasure_code/ec_base.c
+++ b/erasure_code/ec_base.c
@@ -47,8 +47,12 @@ ec_init_tables_base(int k, int rows, unsigned char *a, unsigned char *g_tbls)
         }
 }
 
+#ifdef ISAL_SYMBOL_PREFIX
+unsigned char isal_gf_mul(unsigned char a, unsigned char b)
+#else
 unsigned char
 gf_mul(unsigned char a, unsigned char b)
+#endif
 {
 #ifndef GF_LARGE_TABLES
         int i;
diff --git a/include/erasure_code.h b/include/erasure_code.h
index 17938cf..5ab0c0c 100644
--- a/include/erasure_code.h
+++ b/include/erasure_code.h
@@ -1061,8 +1061,13 @@ gf_6vect_mad_avx2(int len, int vec, int vec_i, unsigned char *gftbls, unsigned c
  * @returns  Product of a and b in GF(2^8)
  */
 
+#ifdef ISAL_SYMBOL_PREFIX
+#define gf_mul isal_gf_mul
+unsigned char isal_gf_mul(unsigned char a, unsigned char b);
+#else
 unsigned char
 gf_mul(unsigned char a, unsigned char b);
+#endif
 
 /**
  * @brief Single element GF(2^8) inverse.
